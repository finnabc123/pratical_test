import { TempereaturePipe } from './tempereature.pipe';

describe('TempereaturePipe', () => {
  it('create an instance', () => {
    const pipe = new TempereaturePipe();
    expect(pipe).toBeTruthy();
  });
});
